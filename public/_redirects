# Missing hashed assets must 404 — never SPA-fallback to index.html (MIME text/html
# breaks ES modules: "Expected a JavaScript module but server responded with text/html").
/assets/*  /404-asset.html  404

# Vue Router history mode
/*    /index.html   200
